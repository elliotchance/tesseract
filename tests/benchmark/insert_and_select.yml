benchmark:
  1_setup:
    sql: DELETE FROM bigtable

  2_insert_1000_records:
    sql: 'INSERT INTO bigtable {"id": %iteration%}'
    repeat: 1000

  3_select_by_id:
    sql: SELECT * FROM bigtable WHERE id = %iteration%
    repeat: 1000
