tests:
  fail_1:
    sql: SELECT
    error: Expected expression after SELECT.

  fail_2:
    sql: SELECT *
    error: Missing FROM clause.

  fail_3:
    sql: SELECT * FROM
    error: Expected table name after FROM.
